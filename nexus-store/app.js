const $=s=>document.querySelector(s);const $$=s=>[...document.querySelectorAll(s)];const page=document.body.dataset.page||'home';let apps=[];let deferredPrompt=null;const ua=navigator.userAgent||'';const isAndroid=/Android/i.test(ua);const isIOS=/iPhone|iPad|iPod/i.test(ua)||(navigator.platform==='MacIntel'&&navigator.maxTouchPoints>1);const saved=new Set(JSON.parse(localStorage.getItem('nexus.saved')||'[]'));const esc=(v='')=>String(v).replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));const initials=n=>n.split(/\s|-/).filter(Boolean).slice(0,2).map(x=>x[0]).join('').toUpperCase();const href=a=>`./app.html?id=${encodeURIComponent(a.id)}`;function icon(a,large=false){return `<div class="app-icon${large?' app-icon-large':''}" style="--c1:${a.colors?.[0]||'#253858'};--c2:${a.colors?.[1]||'#5b8cff'}">${esc(initials(a.name))}</div>`}function platform(a){if(isAndroid&&a.android)return'Android';if(isIOS&&a.ios)return'iPhone / iPad';if(a.web)return'Web';if(a.android)return'Android';return'Bientôt'}function action(a){if(isAndroid&&a.android)return['Installer',a.android];if(isIOS&&a.ios)return['Installer',a.ios];if(a.web)return['Ouvrir',a.web];if(a.android)return['Télécharger',a.android];return['Bientôt',null]}function card(a){const [label,url]=action(a);return `<article class="app-row"><a class="app-link" href="${href(a)}">${icon(a)}<div class="app-copy"><h3>${esc(a.name)}</h3><div class="meta">${esc(a.category)} · ${esc(a.version||'')}</div><p>${esc(a.description||'')}</p></div></a><a class="pill-action${url?' ready':''}" ${url?`href="${esc(url)}"`:''}>${label}</a></article>`}function tile(a){return `<a class="app-tile" href="${href(a)}">${icon(a)}<h3>${esc(a.name)}</h3><span>${esc(a.category)}</span></a>`}function saveState(){localStorage.setItem('nexus.saved',JSON.stringify([...saved]))}function install(a){const [label,url]=action(a);if(!url)return;if(url.startsWith('http'))window.open(url,'_blank','noopener');else location.href=url}function renderHome(){const f=$('#featuredApps'),p=$('#popularApps');if(f)f.innerHTML=apps.slice(0,8).map(tile).join('');if(p)p.innerHTML=apps.slice(0,10).map(card).join('')}function renderSearch(){const input=$('#searchInput'),out=$('#searchResults');if(!input||!out)return;const params=new URLSearchParams(location.search);input.value=params.get('q')||'';let filter='all';const go=()=>{const q=input.value.trim().toLowerCase();const rows=apps.filter(a=>(filter==='all'||a.kind===filter||(filter==='web'&&a.web))&&(!q||`${a.name} ${a.category} ${a.description}`.toLowerCase().includes(q)));out.innerHTML=rows.length?rows.map(card).join(''):'<div class="empty-state compact"><div class="empty-icon">⌕</div><h2>Aucun résultat</h2><p>Essaie un autre mot-clé.</p></div>'};input.addEventListener('input',go);$$('#searchChips .chip').forEach(b=>b.addEventListener('click',()=>{$$('#searchChips .chip').forEach(x=>x.classList.toggle('active',x===b));filter=b.dataset.filter;go()}));go()}function renderCategories(){const grid=$('#categoryGrid'),out=$('#categoryApps');if(!grid||!out)return;const cats=[...new Set(apps.map(a=>a.category).filter(Boolean))];grid.innerHTML=cats.map((c,i)=>`<button class="category-card" data-category="${esc(c)}"><span>${['▦','◆','◉','✦','⌘','▣'][i%6]}</span><b>${esc(c)}</b></button>`).join('');const draw=c=>{out.innerHTML=apps.filter(a=>!c||a.category===c).map(card).join('')};grid.addEventListener('click',e=>{const b=e.target.closest('[data-category]');if(!b)return;$$('.category-card').forEach(x=>x.classList.toggle('active',x===b));draw(b.dataset.category)});draw()}function renderLibrary(){const out=$('#libraryApps'),empty=$('#libraryEmpty');if(!out)return;const rows=apps.filter(a=>saved.has(a.id));out.innerHTML=rows.map(card).join('');if(empty)empty.hidden=rows.length>0}function renderDetail(){const id=new URLSearchParams(location.search).get('id');const a=apps.find(x=>x.id===id);const root=$('#appDetail');if(!root)return;if(!a){root.innerHTML='<div class="empty-state"><h1>Application introuvable</h1><a class="primary" href="./">Retour au store</a></div>';return}document.title=`${a.name} · Nexus Store`;const [label,url]=action(a);root.innerHTML=`<section class="detail-hero">${icon(a,true)}<div class="detail-title"><div class="eyebrow">${esc(a.category)}</div><h1>${esc(a.name)}</h1><p>Nexus Tech</p><div class="detail-actions"><button class="primary" id="detailAction" ${url?'':'disabled'}>${label}</button><button class="save-btn${saved.has(a.id)?' saved':''}" id="saveApp">${saved.has(a.id)?'✓ Enregistrée':'♡ Enregistrer'}</button></div></div></section><section class="stats"><div><b>${esc(a.version||'—')}</b><span>Version</span></div><div><b>${platform(a)}</b><span>Plateforme</span></div><div><b>${a.status==='ready'?'Disponible':'Nexus'}</b><span>Statut</span></div></section><section class="detail-body"><h2>À propos</h2><p>${esc(a.description||'')}</p><div class="detail-meta"><div><span>Développeur</span><b>Nexus Tech</b></div><div><span>Catégorie</span><b>${esc(a.category)}</b></div><div><span>Version</span><b>${esc(a.version||'—')}</b></div></div></section>`;$('#detailAction')?.addEventListener('click',()=>install(a));$('#saveApp')?.addEventListener('click',()=>{saved.has(a.id)?saved.delete(a.id):saved.add(a.id);saveState();renderDetail()});const related=$('#relatedApps');if(related)related.innerHTML=apps.filter(x=>x.id!==a.id&&(x.category===a.category||x.kind===a.kind)).slice(0,7).map(tile).join('')}function renderSettings(){const dark=$('#darkMode'),notif=$('#notifications'),auto=$('#autoUpdates');if(dark){dark.checked=localStorage.getItem('nexus.theme')==='dark';dark.addEventListener('change',()=>{localStorage.setItem('nexus.theme',dark.checked?'dark':'light');applyTheme()})}if(auto){auto.checked=localStorage.getItem('nexus.autoUpdates')!=='false';auto.addEventListener('change',()=>localStorage.setItem('nexus.autoUpdates',String(auto.checked)))}if(notif){notif.checked=Notification.permission==='granted';notif.addEventListener('change',async()=>{if(notif.checked&&'Notification'in window)notif.checked=(await Notification.requestPermission())==='granted'})}$('#installPwa')?.addEventListener('click',promptInstall)}function renderAccount(){$('#nexusSignIn')?.addEventListener('click',()=>location.href='./settings.html#account');$('#nexusSignUp')?.addEventListener('click',()=>location.href='./settings.html#account')}function applyTheme(){document.documentElement.dataset.theme=localStorage.getItem('nexus.theme')==='dark'?'dark':''}async function promptInstall(){if(deferredPrompt){deferredPrompt.prompt();await deferredPrompt.userChoice;deferredPrompt=null;return}if(isIOS)alert('Dans Safari, touche Partager puis « Ajouter à l’écran d’accueil ».');else alert('Utilise le menu du navigateur pour installer Nexus Store sur cet appareil.')}async function load(){applyTheme();try{const r=await fetch('./catalog.json',{cache:'no-store'});apps=await r.json()}catch{apps=[]}({home:renderHome,search:renderSearch,categories:renderCategories,library:renderLibrary,app:renderDetail,settings:renderSettings,account:renderAccount}[page]||(()=>{}))()}window.addEventListener('beforeinstallprompt',e=>{e.preventDefault();deferredPrompt=e});if('serviceWorker'in navigator)navigator.serviceWorker.register('./sw.js').catch(()=>{});load();