export async function createGame(env){const{canvas,ctx,sfx}=env;let pad=0,score=0,raf,running=true;const total=10;const reset=()=>{pad=0;score=0;running=true};const resize=()=>{const r=canvas.getBoundingClientRect(),d=Math.min(globalThis.devicePixelRatio||1,2);canvas.width=Math.floor(r.width*d);canvas.height=Math.floor(r.height*d);ctx.setTransform(d,0,0,d,0,0);return r};const tap=e=>{if(!running)return;const r=canvas.getBoundingClientRect(),x=e.clientX-r.left,y=e.clientY-r.top,cx=r.width*.2+(pad%4)*r.width*.2,cy=120+Math.floor(pad/4)*110;if(Math.hypot(x-cx,y-cy)<40){score++;pad++;sfx?.score?.();if(pad>=total){running=false;env.end?.(true,'Pond crossed')}}else sfx?.tap?.()};const render=()=>{const r=resize();ctx.fillStyle='#0d2a38';ctx.fillRect(0,0,r.width,r.height);ctx.textAlign='center';ctx.fillStyle='#fff';ctx.font='800 21px system-ui';ctx.fillText('LILY PAD HOP DUEL',r.width/2,32);ctx.font='600 13px system-ui';ctx.fillStyle='#b8dde8';ctx.fillText(`Hop ${pad}/${total}`,r.width/2,54);for(let i=0;i<total;i++){const x=r.width*.2+(i%4)*r.width*.2,y=120+Math.floor(i/4)*110;ctx.beginPath();ctx.ellipse(x,y,34,22,0,0,Math.PI*2);ctx.fillStyle=i<pad?'#355c49':i===pad?'#8be28e':'#5ea96d';ctx.fill()}raf=requestAnimationFrame(render)};reset();canvas.addEventListener('pointerdown',tap);raf=requestAnimationFrame(render);return{restart:reset,getState:()=>({pad,score,running}),destroy(){running=false;cancelAnimationFrame(raf);canvas.removeEventListener('pointerdown',tap)}}}export default{createGame};