export async function createGame(env){const{canvas,ctx,sfx}=env;const rooms=[{name:'Hall',hint:'The draft comes from the east wall.'},{name:'Study',hint:'A loose book hides a brass key.'},{name:'Basement',hint:'Three switches must glow in order.'}];let room=0,clues=0,running=true,raf;const reset=()=>{room=0;clues=0;running=true};const resize=()=>{const r=canvas.getBoundingClientRect(),d=Math.min(globalThis.devicePixelRatio||1,2);canvas.width=Math.floor(r.width*d);canvas.height=Math.floor(r.height*d);ctx.setTransform(d,0,0,d,0,0);return r};const tap=e=>{if(!running)return;const r=canvas.getBoundingClientRect(),y=e.clientY-r.top;if(y>r.height*.58&&y<r.height*.82){clues++;sfx?.tap?.();if(clues>=2){room++;clues=0;sfx?.score?.();if(room>=rooms.length){running=false;env.end?.(true,'You escaped the house')}}}};const hint=()=>rooms[Math.min(room,rooms.length-1)]?.hint||'Exit found.';const render=()=>{const r=resize();ctx.fillStyle='#080b16';ctx.fillRect(0,0,r.width,r.height);ctx.textAlign='center';ctx.fillStyle='#f1f3ff';ctx.font='800 21px system-ui';ctx.fillText('NIGHT ALONE ESCAPE',r.width/2,34);ctx.font='600 14px system-ui';ctx.fillStyle='#9ea8c8';ctx.fillText(running?rooms[room].name:'Escaped',r.width/2,62);ctx.fillStyle='#161d31';ctx.fillRect(r.width*.15,r.height*.58,r.width*.7,r.height*.24);ctx.strokeStyle='#55607d';ctx.strokeRect(r.width*.15,r.height*.58,r.width*.7,r.height*.24);ctx.fillStyle='#dfe5ff';ctx.font='600 15px system-ui';ctx.fillText('Search this area',r.width/2,r.height*.70);ctx.font='500 12px system-ui';ctx.fillStyle='#8792b0';ctx.fillText(`Clues ${clues}/2`,r.width/2,r.height*.76);raf=requestAnimationFrame(render)};reset();canvas.addEventListener('pointerdown',tap);raf=requestAnimationFrame(render);return{restart:reset,hint,getState:()=>({room,clues,running,hint:hint()}),destroy(){running=false;cancelAnimationFrame(raf);canvas.removeEventListener('pointerdown',tap)}}}export default{createGame};